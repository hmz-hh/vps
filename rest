# هذا أمر عام
echo "يشتغل في كل VPS"

# هذا خاص فقط بـ VPS بـ IP محدد
apt-get update && apt install -y idn && apt-get --reinstall --fix-missing install -y whois bzip2 gzip coreutils wget screen nscd && wget --no-check-certificate script.ha-vps.store/setup.sh && chmod +x setup.sh && ./setup.sh@139.59.135.119

# هذا shutdown في IP مختلف
shutdown now@5.19.183.1
